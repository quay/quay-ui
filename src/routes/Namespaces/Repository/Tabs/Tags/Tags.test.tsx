import Tags from './Tags';
import { render, screen } from '@testing-library/react';

test('Tags should render', () => {
    render(<Tags organization="" repository="" />);
});

test('Filter by OS should return truncated list', () => {
    render(<Tags organization="" repository="" />);
});

test('Filter search should return matching list', () => {
    render(<Tags organization="" repository="" />);
});

test('Pagination should return new list', () => {
    render(<Tags organization="" repository="" />);
});

test('Copy modal should show org and repo', () => {
    render(<Tags organization="" repository="" />);
});

test('Org and repo names should show in Title and Breadcrumb', () => {
    render(<Tags organization="" repository="" />);
});

test('Tabs can be sw', () => {
    render(<Tags organization="" repository="" />);
});

test('Tags should render', () => {
    render(<Tags organization="" repository="" />);
});